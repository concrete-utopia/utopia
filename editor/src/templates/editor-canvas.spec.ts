describe('Pressing the spacebar whilst in Select mode', () => {
  it('computes the stack of elements under the mouse', () => {})
  it('should select the first element in the stack if no element is selected', () => {})
  it('should select the next element in the stack', () => {})
  it('should select the first element in the stack if the last element is selected', () => {})
})

describe('Hiding an element', () => {
  it('should update the editor session model on the Swift side', () => {})
  it('should add the element to the list of top level hidden components', () => {})
  it("should only render the corners of the element's frame if it is a top level hidden component", () => {})
  it('should not render the element at all if it is a descendant of a hidden element', () => {})
  it('should prevent the element from being targeted via the Canvas', () => {})
  it('should prevent the element from being selected via the Canvas', () => {})
  it('should prevent the element from being a possible parent when inserting a new element', () => {})
  it('should prevent the element from being used when generating guidelines', () => {})
})
