describe('Set element prop via the data picker', () => {
  it('can pick from the property data picker', () => {
    // set up project with vars in scope
    // open the prop picker
    // click the second option
    // check the string on the screen
  })
})
